# GAME_POLICY

## 게임 기본 정책

- 카카오 로그인 회원은 게임을 1회만 진행할 수 있으며, 완료·실패 후 재도전할 수 없다.
- 디자이너 닉네임과 디자인 방향은 게임 시작 전에 각각 최초 1회 저장한다.
- 앱 종료 후에도 게임 진행 상태와 대화 기록을 유지한다.

## 디자인 방향

- `TRAVEL`: 이동이 많은 여행자를 위한 제품
- `HANDS_FREE`: 두 손을 자유롭게 사용할 수 있는 제품
- `DAILY_TRAVEL`: 일상과 여행에서 모두 사용하는 제품

선택한 방향은 `GameProgress`에 저장하며, 상품 추천 시 프론트 요청값이 아닌 로그인 회원의 저장값을 사용한다.

## 사건 및 게임 상태

- 사건은 `FUNCTION`, `SIGNATURE` 두 개이며 `FUNCTION` 성공 후에만 `SIGNATURE`를 진행한다.
- 최종 추리 오답 시 즉시 `FAILED`가 되며, 다른 사건은 진행할 수 없다.
- 두 사건을 모두 성공하면 `COMPLETED`가 되고 디자이너 패스 발급 대상이 된다.

| 상태 | 의미 |
| --- | --- |
| `NOT_STARTED` | 디자인 방향 선택 후 사건 시작 전, 또는 FUNCTION 성공 후 SIGNATURE 선택 전 |
| `IN_PROGRESS` | 현재 사건 진행 중 |
| `FAILED` | 최종 추리 오답으로 게임 종료 |
| `COMPLETED` | 두 사건을 모두 성공하여 게임 완료 |

정답 용의자는 현재 구현 기준으로 `FUNCTION`의 `EMIL`, `SIGNATURE`의 `JOHANNES`다.

## 용의자 심문

- `SIGNATURE`: `CLARA`, `JOHANNES`; `FUNCTION`: `FELIX`, `EMIL`만 심문할 수 있다.
- 캐릭터별 자유 질문은 최대 3회이며, 질문과 AI 답변을 순서대로 저장한다.
- 3번째 질문과 답변 저장 후 대화 상태는 `COMPLETED`가 되며 추가 질문은 차단한다.
- 질문 횟수가 남아 있어도 대화를 조기 종료할 수 있으며, 이 경우에도 상태는 `COMPLETED`가 된다.
- 질문 3회를 모두 사용하거나 두 대화를 완료하지 않아도 현재 사건의 최종 추리는 가능하다.
- AI 호출 실패 시 질문·답변·질문 횟수는 저장하거나 소비하지 않는다.
- AI 호출은 DB 트랜잭션 밖에서 수행하고, 응답 저장 전에 최신 대화 상태를 다시 검증한다.

## 인증 및 회원

- 카카오 OAuth callback은 짧은 수명의 일회용 Login Code만 프론트에 전달하고, 교환 API에서 서비스 Access/Refresh JWT를 발급한다.
- Refresh Token은 해시로 저장하며 재발급 시 기존 토큰을 교체하는 Rotation을 사용한다.
- 로그아웃은 저장된 Refresh Token을 폐기한다. 이미 발급된 Access Token은 만료 전까지 별도로 블랙리스트 처리하지 않는다.
- 회원탈퇴는 MCM 서비스 회원과 연관된 Refresh Token, 게임, 대화·메시지, 디자이너 패스를 삭제한다. 카카오 계정 자체는 탈퇴되지 않는다.

## 완료 보상 및 상품 추천

- 두 사건 완료 시 회원별 고유 디자이너 패스를 발급하며 마이페이지에서 조회한다.
- 상품 추천은 `COMPLETED` 회원에게만 제공하며 `recommended_products`의 디자인 방향별 `display_order` 순서를 사용한다.
- `recommended_products`는 회원 탈퇴나 게임 데이터 삭제 대상이 아닌 공용 기준 데이터다.
