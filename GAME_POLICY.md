# GAME_POLICY

## 게임 기본 정책

* 카카오 로그인 사용자 기준 1인 1회 플레이만 허용한다.
* 디자이너 닉네임은 게임 시작 전 최초 1회 저장한다.
* 게임 진행 중 앱을 종료해도 기존 진행 상태를 유지하고 재접속 시 이어서 진행한다.
* 게임 성공 또는 실패 후에는 재도전할 수 없다.

## 디자인 선택 정책

* 디자이너 닉네임 설정 후 사용자는 3가지 디자인 방향 중 하나를 선택한다.
* 디자인 방향은 다음과 같이 관리한다.

    * `TRAVEL`: 이동이 많은 여행자를 위한 제품
    * `HANDS_FREE`: 두 손을 자유롭게 사용할 수 있는 제품
    * `DAILY_TRAVEL`: 일상과 여행에서 모두 사용하는 제품
* 사용자가 선택한 디자인 방향은 DB에 저장한다.
* 게임 진행 중 선택한 디자인 방향은 변경하지 않는 것을 기본으로 한다.
* 앱 종료 후 재접속하더라도 기존에 선택한 디자인 방향을 유지한다.

## 사건 선택 및 진행 상태 정책

* 게임은 `SIGNATURE`, `FUNCTION` 두 사건으로 구성한다.
* 사용자는 두 사건 중 하나를 먼저 선택하여 진행할 수 있다.
* 현재 진행 중인 사건과 각 사건의 성공 여부를 DB에 저장한다.
* 한 사건을 성공하면 남은 사건을 이어서 진행할 수 있다.
* 진행 중인 사건의 최종 추리가 오답이면 즉시 전체 게임을 실패 처리한다.
* 한 사건에서 실패하면 다른 사건에는 진입할 수 없다.
* 두 사건 모두 성공해야 전체 게임을 최종 성공 처리한다.
* 최종 성공 사용자만 디자이너 패스로 이어질 수 있다.
* 게임 실패 후에도 기존 용의자 대화 기록은 삭제하지 않고 보존한다.
* 사건 진행 중 앱을 종료해도 재접속 시 기존 진행 상태를 이어서 진행한다.

### 전체 게임 상태

* 전체 게임 상태는 `GameStatus`로 관리한다.

    * `NOT_STARTED`: 아직 게임을 시작하지 않은 상태
    * `IN_PROGRESS`: 게임을 시작하여 진행 중인 상태
    * `FAILED`: 사건의 최종 추리에 실패하여 전체 게임이 종료된 상태
    * `COMPLETED`: `SIGNATURE`, `FUNCTION` 두 사건을 모두 성공하여 전체 게임을 완료한 상태
* `NOT_STARTED` 상태에서만 최초 게임 시작이 가능하다.
* `IN_PROGRESS` 상태에서는 새 게임을 생성하거나 초기화하지 않고 기존 게임을 이어서 진행한다.
* `FAILED`, `COMPLETED` 상태에서는 새로운 게임을 시작하거나 재도전할 수 없다.

## 용의자 심문 정책

* 용의자는 `CLARA`, `JOHANNES`, `FELIX`, `EMIL` 총 4명이다.
* 각 용의자에게 최대 3번까지 자유롭게 질문할 수 있다.
* 사용자 질문과 AI 캐릭터 답변을 모두 DB에 저장한다.
* 사용자 질문과 AI 답변은 순서대로 저장하여 재접속 시 기존 대화 내역을 그대로 조회할 수 있어야 한다.
* 캐릭터별 질문 횟수는 서로 독립적으로 관리한다.
* 한 캐릭터에게 사용한 질문 횟수는 다른 캐릭터의 질문 횟수에 영향을 주지 않는다.
* 1~2회 질문 후 앱이나 대화 화면을 종료하면 해당 대화는 `IN_PROGRESS` 상태를 유지한다.
* 3번째 질문과 AI 답변 저장이 정상 완료되면 해당 대화는 자동으로 `COMPLETED` 상태가 된다.
* `COMPLETED` 상태에서는 추가 질문을 허용하지 않는다.
* 완료된 대화의 기존 기록은 계속 조회할 수 있다.
* API 응답에는 현재 질문 수, 최대 질문 수, 남은 질문 수를 포함한다.
* 현재는 대화 시간 제한을 적용하지 않는다.

## 사건별 등장인물 정책

* 각 사건에서 심문할 수 있는 캐릭터는 고정한다.
* 각 캐릭터는 자신이 배정된 사건에서만 심문할 수 있다.

### SIGNATURE 사건

* `CLARA`: 패턴 장인 클라라
* `JOHANNES`: 사진작가 요하네스
* `SIGNATURE` 사건에서는 `CLARA`, `JOHANNES`만 심문할 수 있다.
* 두 캐릭터는 각각 최대 3번까지 자유롭게 질문할 수 있다.
* `SIGNATURE` 사건에서 `FELIX`, `EMIL`에게 질문하는 요청은 백엔드에서 차단한다.

### FUNCTION 사건

* `FELIX`: 제품 설계자 펠릭스
* `EMIL`: 테스트 담당자 에밀
* `FUNCTION` 사건에서는 `FELIX`, `EMIL`만 심문할 수 있다.
* 두 캐릭터는 각각 최대 3번까지 자유롭게 질문할 수 있다.
* `FUNCTION` 사건에서 `CLARA`, `JOHANNES`에게 질문하는 요청은 백엔드에서 차단한다.

### 캐릭터 대화 공통 규칙

* 사건 하나에서 최대 질문 수는 6회이다.
* 전체 게임에서 최대 질문 수는 12회이다.
* 각 캐릭터의 사용자 질문과 AI 답변은 해당 캐릭터의 대화 기록으로 구분하여 저장한다.

## AI 대화 정책

* 용의자와의 대화는 선택지 방식이 아닌 자유 대화 방식으로 진행한다.
* 사용자가 입력한 자유 질문을 해당 캐릭터의 대화 맥락과 함께 AI에 전달하는 것을 기본으로 한다.
* 각 캐릭터의 페르소나와 AI 응답 방식은 추후 AI 파트와 연동하여 구체화할 수 있다.
* AI 연동 방식이 변경되더라도 사용자 질문, AI 답변, 질문 횟수, 대화 상태 저장 구조는 유지하는 것을 기본으로 한다.

## API 문서화 정책

* Swagger의 한국어 설명은 UTF-8 실제 한글로 작성하며 `\uXXXX` 형태로 작성하지 않는다.
* Enum을 사용하는 API는 가능한 Enum 값과 각 값의 의미를 Swagger에서 명확히 설명한다.
* 프론트엔드가 별도 확인 없이 사용할 수 있도록 요청값, 응답값, 호출 가능한 상태와 주요 오류 상황을 명확히 설명한다.
